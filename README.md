# 📚 Online Library

Веб-застосунок "Онлайн-бібліотека" для зручного доступу до цифрових літературних ресурсів.

## 🛠 Технології

### Backend

- **Express.js** - веб-фреймворк для Node.js
- **Prisma ORM** - робота з базою даних
- **PostgreSQL** - реляційна база даних
- **Redis** - кешування для покращення продуктивності
- **ioredis** - Redis клієнт для Node.js
- **JWT** - аутентифікація
- **bcryptjs** - хешування паролів
- **Swagger UI** - інтерактивна API документація

### Frontend

- **React.js** - UI бібліотека
- **Vite** - збірка та dev-сервер
- **React Router** - маршрутизація
- **TanStack Query** - управління серверним станом
- **Zustand** - глобальний стан
- **React Hook Form** - форми
- **Lucide React** - іконки

### Зовнішній API

- **Open Library API** - каталог книг (20+ мільйонів)

## 📁 Структура проєкту

```
online-library/
├── backend/
│   ├── prisma/
│   │   ├── schema.prisma    # Схема бази даних
│   │   └── seed.js          # Початкові дані
│   ├── src/
│   │   ├── config/          # Конфігурація
│   │   ├── controllers/     # Контролери (MVC)
│   │   ├── middleware/      # Middleware
│   │   ├── routes/          # Роути API
│   │   ├── services/        # Бізнес-логіка
│   │   ├── utils/           # Утиліти
│   │   └── app.js           # Точка входу
│   ├── uploads/             # Завантажені файли
│   └── package.json
│
├── frontend/
│   ├── src/
│   │   ├── api/             # API клієнти
│   │   ├── components/      # React компоненти
│   │   ├── context/         # Глобальний стан
│   │   ├── hooks/           # Custom hooks
│   │   ├── pages/           # Сторінки
│   │   ├── styles/          # Глобальні стилі
│   │   ├── App.jsx          # Головний компонент
│   │   └── main.jsx         # Точка входу
│   └── package.json
│
└── README.md
```

## 🚀 Запуск проєкту

### Передумови

- Node.js 18+
- Docker (рекомендовано) або PostgreSQL 14+ та Redis 6+
- npm або yarn

### 1. Налаштування бази даних та Redis

**Варіант A: Через Docker (рекомендовано)**

```bash
# З кореневої папки проєкту
docker compose up -d

# Перевірка статусу
docker compose ps
```

**Варіант B: Локальний PostgreSQL та Redis**

```bash
# Створіть базу даних PostgreSQL
createdb online_library

# Запустіть Redis локально (залежно від ОС)
# macOS: brew install redis && brew services start redis
# Ubuntu: sudo apt-get install redis-server && sudo systemctl start redis
# Windows: встановіть Redis через WSL або використовуйте Docker
```

### 2. Backend

```bash
cd backend

# Встановлення залежностей
npm install

# Налаштуйте .env файл
# DATABASE_URL="postgresql://user:password@localhost:5432/online_library"
# JWT_SECRET="your-secret-key"
# REDIS_HOST="localhost" (опціонально, за замовчуванням: localhost)
# REDIS_PORT="6379" (опціонально, за замовчуванням: 6379)
# REDIS_PASSWORD="" (опціонально, якщо Redis потребує пароль)
#
# Примітка: Якщо Redis недоступний, сервер продовжить працювати без кешування

# Генерація Prisma клієнта
npm run db:generate

# Міграція бази даних
npm run db:push

# Заповнення тестовими даними
npm run db:seed

# Запуск сервера
npm run dev
```

Backend буде доступний на http://localhost:5000

### 3. Frontend

```bash
cd frontend

# Встановлення залежностей
npm install

# Запуск dev-сервера
npm run dev
```

Frontend буде доступний на http://localhost:5173

## 📘 API Документація (Swagger)

Після запуску backend сервера, Swagger UI доступний за адресою:

**http://localhost:5000/api-docs**

Swagger надає інтерактивну документацію всіх API ендпоінтів, де ви можете:

- Переглянути всі доступні ендпоінти
- Побачити структуру запитів та відповідей
- Протестувати API безпосередньо з браузера
- Авторизуватись та використовувати захищені ендпоінти

### Використання авторизації в Swagger

1. Спочатку виконайте запит `/api/auth/login` через Swagger
2. Скопіюйте отриманий `token` з відповіді
3. Натисніть кнопку "Authorize" (🔓) у верхній частині Swagger UI
4. Введіть `Bearer <ваш_токен>` (без лапок, з пробілом після Bearer)
5. Натисніть "Authorize", потім "Close"
6. Тепер всі захищені ендпоінти будуть використовувати ваш токен автоматично

## 📖 API Endpoints

### Auth

- `POST /api/auth/register` - Реєстрація
- `POST /api/auth/login` - Вхід
- `GET /api/auth/me` - Профіль
- `PUT /api/auth/profile` - Оновлення профілю
- `PUT /api/auth/password` - Зміна пароля

### Books

- `GET /api/books` - Список книг
- `GET /api/books/search` - Пошук книг
- `GET /api/books/trending` - Популярні книги
- `GET /api/books/:id` - Деталі книги
- `POST /api/books/:id/download` - Завантаження

### Categories

- `GET /api/categories` - Список категорій
- `GET /api/categories/:id/books` - Книги за категорією

### Library (User)

- `GET /api/library` - Збережені книги
- `POST /api/library/:bookId` - Зберегти книгу
- `DELETE /api/library/:bookId` - Видалити зі збережених

### Admin

- `GET /api/admin/stats` - Статистика
- `GET /api/admin/users` - Користувачі
- `POST /api/books` - Створити книгу
- `PUT /api/books/:id` - Оновити книгу
- `DELETE /api/books/:id` - Видалити книгу

## 👥 Ролі користувачів

### Гість

- Перегляд книг та категорій
- Пошук книг
- Реєстрація/Вхід

### Користувач (USER)

- Все що може гість
- Збереження книг у бібліотеку
- Завантаження книг
- Управління профілем

### Адміністратор (ADMIN)

- Все що може користувач
- Управління книгами (CRUD)
- Управління категоріями
- Управління користувачами
- Перегляд статистики

## 🔑 Демо-акаунти

| Роль  | Email             | Пароль   |
| ----- | ----------------- | -------- |
| Admin | admin@library.com | admin123 |
| User  | user@library.com  | user123  |

## 📝 Ліцензія

MIT License
